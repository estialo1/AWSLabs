Step 1
In VScode terminal,
Navigate to the folder with name "AWSLabs"
Run command git clone https://github.com/awscloudessentials/intro-to-aws-lab.git (this will clone this repo into your local machine)
Run command cd intro-to-aws-lab
Run command ls -la to view content of local repo
Copy Lab3a.txt into the folder "AWSLabs"
Delete the cloned repo

Step2  (Create a Virtual Server)
Login into your AWS Console
Create a Launch template
Select your default VPC
With the following  Inbound rule
Type      Source
SSH         Anywhere
HTTP       Anywhere IPv4
Select “Amazon Linux 2023 AMI “ under Application and OS Images
Select “t3micro “ for instance type
Key pair with name "Linux" and key file format ".pem"
Download pem file into your local machine
Create a Security Group with name “PublicSG” and give it any description you prefer
Scroll down to the section with userdata copy and paste this:

#!/bin/bash
yum update -y
yum install -y httpd
systemctl start httpd
systemctl enable httpd

BOOT_TIME=$(date '+%Y-%m-%d %H:%M:%S UTC')
INSTANCE_ID=$(curl -s http://169.254.169.254/latest/meta-data/instance-id)
AZ=$(curl -s http://169.254.169.254/latest/meta-data/placement/availability-zone)

cat > /var/www/html/index.html << EOF
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>ASG Demo</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #0f1923;
      font-family: "Segoe UI", Arial, sans-serif;
      color: #e8edf2;
    }
    .card {
      background: #1a2535;
      border: 1px solid #2a3a50;
      border-radius: 16px;
      padding: 48px 56px;
      text-align: center;
      max-width: 460px;
      width: 90%;
    }
    .label {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: #5b8db8;
      margin-bottom: 12px;
    }
    .timestamp {
      font-size: 28px;
      font-weight: 700;
      color: #ff9900;
      margin-bottom: 8px;
      line-height: 1.3;
    }
    .sub {
      font-size: 14px;
      color: #7a9ab8;
      margin-bottom: 36px;
    }
    .divider {
      border: none;
      border-top: 1px solid #2a3a50;
      margin-bottom: 28px;
    }
    .meta { text-align: left; }
    .row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 12px;
    }
    .key {
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #4a6a88;
    }
    .val {
      font-size: 12px;
      color: #c0d0e0;
    }
    .badge {
      display: inline-block;
      margin-top: 28px;
      background: #1f3a1f;
      border: 1px solid #2d5a2d;
      color: #5db85d;
      font-size: 12px;
      font-weight: 600;
      padding: 6px 16px;
      border-radius: 20px;
    }
  </style>
</head>
<body>
  <div class="card">
    <p class="label">New instance spun up by ASG</p>
    <div class="timestamp">${BOOT_TIME}</div>
    <p class="sub">Terminate this instance — the ASG will replace it with a newer timestamp</p>
    <hr class="divider">
    <div class="meta">
      <div class="row">
        <span class="key">Instance ID</span>
        <span class="val">${INSTANCE_ID}</span>
      </div>
      <div class="row">
        <span class="key">Availability Zone</span>
        <span class="val">${AZ}</span>
      </div>
    </div>
    <span class="badge">Auto Scaling Group is healthy</span>
  </div>
</body>
</html>
EOF

NB: Enable "Assign public IP"
Then create the launch template.

Step3
Create an Auto Scaling Group(ASG) using the launch template you created in Step2
Select no scaling policy and use Minimum -1
Desired - 2
Maximum - 3
Once done, move to step 4

Step 4
Go to your EC2 dashboard, wait for the 2 instances to come up and then terminate them
Send a screenshot showing the instances terminating and 2 new instances spun up by the ASG and share on WhatsApp
Once confirmed, delete your ASG

Step 4 (Documentation)
Modify and update every step you took to achieve the lab, that was not captured in the lab instruction in your local repo Lab3a.txt file and save.
On the terminal run command git add Lab3a.txt
Git commit -m “use a meaningful message here"
Push to your github repo


AWS Lab 3a Documentation

Step 1: Clone and Prepare Files
I navigated to my `AWSLabs` folder in VS Code terminal:
``bash
cd AWSLabs
``
I cloned the repository:
``bash
git clone https://github.com/awscloudessentials/intro-to-aws-lab.git
``
I entered the repo and checked its contents:

``bash
cd intro-to-aws-lab
ls -la
``
I copied `Lab3a.txt` into the `AWSLabs` folder and deleted the cloned repository:

``bash
cd ..
rm -rf intro-to-aws-lab
``

Step 2: Create Launch Template**
I logged into AWS and opened the EC2 dashboard.
I created a Launch Template with the following settings:

* AMI: Amazon Linux 2023
* Instance type: t3.micro
* Key pair: Linux (.pem)
* VPC: Default
* Enabled auto-assign public IP
I created a security group named **PublicSG** and allowed:
* SSH from Anywhere
* HTTP from Anywhere IPv4
I added the user data script to install and start Apache.
I then created the Launch Template.

Step 3: Create Auto Scaling Group
I created an Auto Scaling Group using the Launch Template.
I configured:
* Minimum capacity: 1
* Desired capacity: 2
* Maximum capacity: 3
* Scaling policy: None

Step 4: Test Auto Scaling**
I went to the EC2 dashboard and waited for 2 instances to run.
I terminated both instances.
The Auto Scaling Group automatically launched 2 new instances.
I confirmed that the ASG was working correctly and took a screenshot.

Step 5: Push Documentation
I updated my `Lab3a.txt` file with my steps.
I staged, committed, and pushed my changes:

``bash
git add Lab3a.txt
git commit -m "Updated Lab3a documentation"
git push origin main
``