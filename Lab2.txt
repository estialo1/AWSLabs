Step 1
In VScode terminal,cd into the directory "intro-to-aws-lab"and run command "git pull https://github.com/awscloudessentials/intro-to-aws-lab.git" to pull the Lab2.txt file into your local machine
Open the  .txt file in VScode editor and follow instructions to finish the lab.

Step2  (Create a Key Pair)
Login into your AWS Console
Create a Key pair with name "Linux" and key file format ".pem"
Download pem file into your local machine

Step 3(Create Security group)
Create a Security Group with name “PublicSG” and give it any description you prefer
Select your default VPC
With the following  Inbound rule
Type      Source
SSH         My IP
HTTP       Anywhere IPv4
HTTP       Anywhere IPv6

Step 4 (Create a Launch Template)
Create Launch template , give it a name and description you prefer.
Select “Amazon Linux 2 AMI “ under Application and OS Images
Select “t2micro “ for instance type
Select your pre-created Key pair you created in step 2
Select existing security group and select the one you created in step 3

Open up the “Advanced details” tab and scroll to “User data” and paste the below script
#!/bin/bash
# Please make sure to launch Amazon Linux 2
yum update -y
yum install -y httpd
systemctl start httpd
systemctl enable httpd
echo "Hello World from $(hostname -f)" > /var/www/html/index.html
echo "Healthy" > /var/www/html/health.html
Click create launch template

Step 5 (Documentation)
Modify and update every step you took to achieve the lab, that was not captured in the lab instruction in your local repo Lab2.txt file and save.
On the terminal run command git add Lab2.txt
Git commit -m “launch template demo”
Push to your personal repo.


Additional Documentation for Lab 2
First, I navigated to the intro-to-aws-lab repository directory in the terminal before running the pull command. This ensured that the latest updates could be retrieved into the correct folder.

cd ~/Desktop/AWSlabs/intro-to-aws-lab

After navigating to the directory, I pulled the latest changes from the remote repository to download the **Lab2.txt** file.
git pull https://github.com/awscloudessentials/intro-to-aws-lab.git

Once the file was downloaded, I opened **Lab2.txt** in Visual Studio Code to review the instructions and complete the required lab tasks.

Next, I logged into the AWS Management Console and navigated to the **EC2 Dashboard** in order to create the required AWS resources such as the key pair, security group, and launch template.

After creating the launch template, I launched an EC2 instance from the template to verify that the configuration worked correctly and that the user data script executed successfully.

To test the web server, I navigated to the **Instances** section in the EC2 dashboard, copied the **Public IPv4 address** of the running instance, and opened it in a web browser.

The browser successfully displayed the message:

Hello World from <hostname>

This confirmed that the Apache web server was installed and running correctly on the instance.

Finally, after updating the documentation in **Lab2.txt**, I staged, committed, and pushed the updated file to my personal repository on GitHub using the following commands:

git add Lab2.txt
git commit -m "launch template demo"
git push origin main

The changes were successfully uploaded and verified in my remote GitHub repository.
